[project]
name = "latent-backend"
version = "0.0.0"
description = "latent API — integrations, agents, RAG"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic-settings>=2.6",
    "httpx>=0.28",
]

[tool.uv]
package = false
